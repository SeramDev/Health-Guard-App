import 'package:flutter/foundation.dart';

// used to show loading indicator
final ValueNotifier isMapLoaded = ValueNotifier<bool>(false);

// used to show bottm sheet
//built in method used
//final ValueNotifier isUserMarkerTapped = ValueNotifier<bool>(false);
