import 'data.dart';
import 'geolocator.dart';

Future<List<MapUser>> getUsers() async {
  try {
    // 1 get current user location
    var currentUserLocation = await currentUserPosition();
    // 2 get 
  } catch (e) {

  }
  
}
